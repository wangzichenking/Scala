package com.scala.chapter04
import scala.math._
object Operate {
    def main(args: Array[String]): Unit = {
        println(1+1)
        //--------------------
        //scala中的运算符可以通过方法调用
        println(1.+(2))
        //var person = new Person()
        //println(person.sum(5)
        //println(person sum 10)
        println(sqrt(100))
    }
}

